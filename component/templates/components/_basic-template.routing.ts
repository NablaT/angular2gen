import { Route } from '@angular/router';
import { <%= nameOfComponent %>Component } from './index';

/**
 * Routing for <%= nameOfComponent %>Component
 */
export const <%= nameOfComponent %>Routes: Route[] = [
    {
        path: '',
        component: <%= nameOfComponent %>Component
    }
];